export type Service = {
  service: string
  description: string
  img_path: string
}

export type HandleMenu = {
  handleMenu: (boolean: boolean) => void
}

export type MenuOpen = {
  menuOpen: boolean
}
